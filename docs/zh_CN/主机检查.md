# 主机检查

首次部署前，建议先执行主机预检查：

```bash
bash ./scripts/check_host.sh
```

该脚本会检查：

- 是否安装了 `docker`
- 是否可执行 `docker compose`
- 是否存在 `.env`
- 是否存在 `machine.env`
- 是否存在 `configs/livox/MID360_config.json`

该脚本不会修改主机配置。
