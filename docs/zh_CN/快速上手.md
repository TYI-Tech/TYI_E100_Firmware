# 快速上手

该目录面向 Ubuntu 20.04 + Docker + Docker Compose 的客户部署环境。
正常情况下，客户只需要修改一个文件：`machine.env`。

## 首次部署

```bash
cd firmware/base_stack_noetic_customer
bash ./scripts/check_host.sh
vim machine.env
bash ./scripts/deploy.sh
```

`deploy.sh` 会按顺序完成三件事：

- 应用当前机型的 UART、MID360 与宿主机网卡配置
- 基于本地源码构建 Docker 镜像
- 以后台方式启动运行容器

`check_host.sh` 会在部署前检查 Docker、Docker Compose 以及关键配置文件是否齐备。

## 部署完成后

日常使用可执行以下命令：

```bash
bash ./scripts/status.sh
bash ./scripts/logs.sh
bash ./scripts/enter.sh
```

如果目标机器上的 Docker 需要管理员权限，脚本会自动回退到 `sudo docker compose`。
